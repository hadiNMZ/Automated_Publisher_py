requests>=2.31.0
python-dotenv>=1.0.0
gTTS>=2.5.0
edge-tts>=7.0.0
arabic-reshaper>=3.0.0
python-bidi>=0.6.0
google-api-python-client>=2.100.0
google-auth-oauthlib>=1.1.0
google-auth-httplib2>=0.1.1
Pillow>=10.0.0

# Only needed if PHRASE_PROVIDER=gemini in .env — uncomment if you use it.
# May conflict with newer protobuf versions in some environments.
# google-generativeai>=0.7.0
